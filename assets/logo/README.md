# Monograma MS

Marca de pincel "MS" do MSCREATIVE.SYSTEMS™. Fonte de verdade canônica.

| Arquivo | Cor | Uso |
|---------|-----|-----|
| `MS.svg` | navy `#0d132a` | `<img>` em fundo claro |
| `MS-currentColor.svg` | herda `currentColor` | inline em HTML/SVG — herda cor e opacidade do CSS pai (qualquer fundo) |
| `MS-white.svg` | branco `#FFFFFF` | `<img>` em fundo escuro |

viewBox: `0 0 67.19667 51.75458` (proporção ~1.3:1, sem container).

## URLs estáveis

Raw GitHub:
```
https://raw.githubusercontent.com/1marcelserrano/design-system/main/assets/logo/MS-currentColor.svg
```

CDN (jsDelivr, com cache):
```
https://cdn.jsdelivr.net/gh/1marcelserrano/design-system@main/assets/logo/MS-currentColor.svg
```

Troque o nome do arquivo na URL para a variante desejada (`MS.svg`, `MS-white.svg`).

## Embed inline (temável)

```html
<svg viewBox="0 0 67.19667 51.75458" width="54" style="color:#B2A898;opacity:.65">
  <use href="https://cdn.jsdelivr.net/gh/1marcelserrano/design-system@main/assets/logo/MS-currentColor.svg#..."/>
</svg>
```

Para herança de `currentColor`, prefira colar o conteúdo do `MS-currentColor.svg` como `<symbol>` reutilizável (ver `stories/` no repo `mscreative-coming-soon`).
